<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>libi_modes</name>
  <version>0.1.0</version>
  <description>LIBI 미션 FSM+BT — py_trees 하나로 8개 상태 전이와 상태별 동작을 구현한다.</description>
  <maintainer email="dlrkdxor0821@gmail.com">leekt</maintainer>
  <license>Proprietary</license>

  <exec_depend>rclpy</exec_depend>
  <exec_depend>std_msgs</exec_depend>
  <!-- [2026-07-29] cmd_vel_hold(잠금 상태 정지 명령) 발행 — state_io.py -->
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>libi_interfaces</exec_depend>
  <exec_depend>ament_index_python</exec_depend>
  <exec_depend>python3-py-trees</exec_depend>
  <exec_depend>python3-yaml</exec_depend>
  <!-- 라이다 노치 도킹 (libi_modes/lidar/) — LaserScan 구독과 통계 연산에 쓴다.
       numpy 는 lidar/ 안에서만 쓴다. BT leaf 와 common/ 에는 새 의존을 만들지 않는다. -->
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>python3-numpy</exec_depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
