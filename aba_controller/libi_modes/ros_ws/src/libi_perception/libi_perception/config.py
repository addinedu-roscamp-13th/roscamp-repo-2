# All values are reference starting points for tuning — not hard requirements.
#
# ══════════════════════════════════════════════════════════════════════════
#  [2026-08-02] **단위는 카메라 원본 픽셀 그대로다. 환산 없다.**
#
#  예전에는 `pid.py` 가 들어온 좌표에 `k = IMAGE_WIDTH / image_width` 를 곱해
#  640 기준으로 끌어올려 계산했다. 그래서 화면에서 눈으로 잰 픽셀과 이 표의
#  숫자가 늘 2배 달랐다. **사용자 지시로 그 환산을 걷어냈다** — 이제 검출이
#  보내는 픽셀이 그대로 PID 에 들어간다.
#
#  아래 픽셀 값은 전부 **320×240 스트림 기준**이다. 화면에서 잰 값이 곧 이 값이다.
#
#  ⚠️ **카메라 해상도를 바꾸면 이 표를 손으로 다시 잡아야 한다.** 자동으로
#     맞춰 주던 장치가 없어졌다. 2026-07-30 에 640 → 320 으로 내렸을 때
#     실제로 겪은 증상이 기준점이다:
#       · 거리: sqrt(area) 가 선형으로 작아져 화면을 꽉 채워도 목표에 못 닿고
#               **전진이 안 멈춘다**
#       · 조향: 화면 중심 가정이 어긋나 **한쪽으로 계속 돈다**
#     해상도를 바꾸는 날 고칠 것: TARGET_SIZE, DIST_DEADZONE, IMAGE_WIDTH,
#     ANGLE_DEADZONE, INTEGRAL_*_CLAMP (길이에 비례) / KP_DIST, KI_DIST,
#     KP_ANGLE (길이에 반비례).
# ══════════════════════════════════════════════════════════════════════════

# ---- distance PID (linear_x) ----
# [2026-07-28] 360 → 180. **360 은 사실상 도달 불가능한 목표였다 — 로봇이 들이박았다.**
#
#   size = sqrt(bbox 폭 × 높이) 이고 거리에 반비례한다.
#   사람(키 1.7m·폭 0.45m)이 화면 높이를 **정확히 꽉 채우는** 거리에서 320×240 이면:
#       h = 240,  w ≈ 0.45/1.7 × 240 = 63.5     size = sqrt(240×63.5) = 123
#   즉 화면을 세로로 가득 채워도 123 이다. 목표가 그보다 크면 오차가 계속 양수라
#   PID 는 bbox 가 잘려 폭이 더 넓어질 때까지 — 사람 코앞까지 — 전진을 멈추지 않는다.
#   후진(LINEAR_X_REVERSE_MAX)은 영영 안 걸린다.
#
#   [2026-08-02] **188 — 사용자가 측정기로 화면에서 직접 잡은 값이다.**
#
#   위 계산(123)은 사람이 **안 잘린 채** 화면 높이를 꽉 채울 때의 값이다. 188 은
#   그보다 크므로, 그 크기가 나오려면 사람이 더 가까워져 **bbox 가 위아래로 잘리고
#   폭이 넓어져야** 한다 — 240 높이로 잘린 상태에서 폭 147px 이면 √(240×147) = 188.
#   실측으로 나온 값이니 도달 가능하다. 화면에서 잰 것이 계산 모형보다 우선한다.
#
#   ⚠️ 그래도 이 축은 사고 이력이 있는 축이다. 목표가 실제로 안 나오는 크기면
#      오차가 계속 양수라 **전진이 영영 안 멈춘다**(2026-07-28·07-30). 만약 로봇이
#      사람에게 계속 붙는다면 이 값이 너무 큰지부터 본다. STOP_DIST 하드 스톱이
#      밑을 받치지만 그건 최후 수단이지 제어가 아니다.
#
#   [2026-08-02 후] **188 → 150 → 169. 150 은 반대로 너무 멀었다.**
#     188(너무 가까움)과 150(너무 멂)의 가운데를 잡았다 — 사용자 지정.
#     169 는 188 대비 1.11배 멀고, 150 대비 0.89배 가깝다.
#
#   ⚠️ 조정 규칙: **거리에 반비례**한다.
#       새 값 = 지금 값 × (지금 멈추는 거리 ÷ 원하는 거리)
#      더 멀리 서게 하려면 **낮추고**, 더 붙게 하려면 올린다.
#      예) 지금 0.8m 에 서는데 1.2m 를 원한다 → 150 × (0.8/1.2) = 100
TARGET_SIZE = 169.0            # sqrt(area) setpoint — px @320(원본) 기준
KP_DIST = 0.0060
KI_DIST = 0.0002
KD_DIST = 0.0
INTEGRAL_DIST_CLAMP = 25.0
# [2026-08-02] **0.12 → 0.08 → 0.06. 계속 너무 빨랐다(실측).**
#   `KP_DIST × e` 는 오차 10px 만 넘어도 상한에 닿는다 — 즉 추종 대부분의 시간을
#   최대속도로 달린다. **상한이 곧 체감 속도다.** 느리면 여기를 올린다.
LINEAR_X_MAX = 0.06            # forward max (m/s)
LINEAR_X_REVERSE_MAX = 0.04   # reverse max (m/s) — smaller: LiDAR blind rear

# [2026-07-31] **거리축 정지 구간 — 되살린 것이다.**
#
#   방위각에는 `ANGLE_DEADZONE` 이 처음부터 있었는데 거리축에는 없었다. 그래서
#   목표에 도달해도 오차가 정확히 0 이 되는 프레임이 없다 — bbox 는 매 프레임
#   몇 픽셀씩 흔들리고, `e=10` 이면 `KP_DIST × 10 = 0.03 m/s` 라 바퀴가 계속
#   앞뒤로 깨작인다. "다 왔는데 왜 안 서지" 가 이것이다.
#
#   ⚠️ **원래 있던 값이다.** `cmd_preview.py` 의 `SIZE_DEADBAND = 30.0`
#      (`TARGET_SIZE = 300` 기준, 지금도 그 파일에 그대로 있다). 그런데 그건
#      bang-bang 프리뷰 경로고, 실제로 바퀴를 돌리는 건 이 PID 다 —
#      `serve_loop` 은 `policy` 가 있으면 `compute_cmd_vel` 을 아예 안 부른다.
#      제어가 PID 로 옮겨갈 때 구간만 안 따라와서 없어진 것이다.
#
#   같은 비율을 옮겨 온다: 30/300 = **목표의 10%**.
#
#   단위는 `sqrt(area)` 픽셀(320 원본 기준)이다. 크기가 거리에 반비례하므로
#   비율이 그대로 거리로 옮겨간다: 목표의 10% 면 **1.0m 에서 ±10cm** 안이면 안 움직인다.
#
#   ⚠️ 조정: 좁히면 더 바짝 따라붙고 더 자주 깨작인다. 넓히면 더 잘 멈추는 대신
#      사람이 그만큼 움직여야 반응한다. **0 이면 끈다**(구간 없던 시절 동작).
#      `pid.py` 는 구간 밖에서 이 값을 오차에서 **빼므로**, 넓혀도 경계에서
#      튀지는 않는다 — 반응이 그만큼 늦어질 뿐이다.
#
#   [2026-08-02] **사용자가 측정기(아티팩트)로 화면에서 직접 친 두 박스다.**
#   전부 320 원본 픽셀 기준:
#
#       "이 정도면 전진"  √area ≈ **158**   (멀다 · bbox 작다)
#       "이 정도면 후진"  √area ≈ **218**   (가깝다 · bbox 크다)
#
#   가운데 (158+218)/2 = **188** → `TARGET_SIZE`
#   반폭   (218-158)/2 = **30**  → 여기 값. 목표의 16%.
#
#   즉 √area 158~218 안이면 안 움직인다. 그 밖은 각각 전진·후진이다.
#
#   ⚠️ 조정: 좁히면 더 바짝 따라붙고 더 자주 반응한다. 넓히면 더 잘 멈추는 대신
#      사람이 그만큼 움직여야 반응한다. **0 이면 끈다.**
#      좁혔을 때 걱정되던 "경계에서 튀는 것"은 `pid.py` 가 오차를 0 으로 죽이는 대신
#      **빼내도록** 고쳐서 없앴다 — 좁혀도 그 문제는 안 돌아온다.
#
#   ⚠️ 여전히 깨작이면 30 을 넓히기 전에 `KP_DIST`(0.0060) 를 먼저 의심할 것.
DIST_DEADZONE = 30.0          # sqrt(area) px @320 = 목표의 16% — 이 안이면 linear_x = 0

# ---- bearing PID (angular_z) ----
# 카메라 원본 폭. **이제 이 값이 곧 계산에 쓰이는 폭이다**(환산 없음).
#   실측: `scripts/all/libi_pi.sh:486` 이 `--width 320` 으로 띄운다 → 320×240.
#   ⚠️ 해상도를 바꾸면 이 값과 아래 픽셀 상수들을 **손으로 같이 고쳐야 한다.**
IMAGE_WIDTH = 320
# [2026-08-02] **0.0020 → 0.0040. 회전이 굼떴다(실측).**
#   데드존을 1/6 → 1/18 로 좁히면서 빼내는 양도 같이 줄었으므로, 같은 화면 위치에서
#   나오는 오차는 오히려 커진다. 그래도 사용자가 "각회전 더 빠르게"를 요구해 2배로.
KP_ANGLE = 0.0040
KI_ANGLE = 0.0
KD_ANGLE = 0.0
INTEGRAL_ANGLE_CLAMP = 100.0
# [2026-08-01] **화면의 3등분 가운데 칸과 같게 맞춘 것이다.**
#
#   서버가 영상에 굽는 가이드선은 `w//3`, `2*w//3` 다(perception_server.draw_overlay).
#   즉 가운데 칸은 중심에서 **±w/6**, 320 기준으로 **±53.33px** 다. 예전에 데드존이
#   가운데 칸의 절반도 안 됐을 때는 사람이 화면상 명백히 가운데 칸 안에 있는데도
#   로봇이 계속 좌우로 깨작였다. "가운데 있는데 왜 움직여"가 이것.
#
#   눈에 보이는 선과 제어의 경계가 다르면 화면이 거짓말을 한다. 같게 둔다.
#
#   ⚠️ 이 값을 넓히려면 `pid.py` 의 방위 데드존이 **오차를 빼내야** 한다. 예전처럼
#      0 으로 죽이면 경계 바로 밖에서 `KP_ANGLE × 53.33 = 0.107 rad/s` 가 통째로
#      튀어나온다. 그래서 거리축과 같은 방식으로 고쳤다.
ANGLE_DEADZONE = 13.33        # px @320 — 폴백. 아래 FRAC 이 있으면 그쪽이 이긴다
# [2026-08-02] **1/6 → 1/18. 사용자 지시로 정지 구간을 1/3 로 좁혔다.**
#
#   근거: **화각이 좁다.** 화면 3등분 가운데 칸(중심 ±w/6 = ±53.3px @320)은 실제
#   각도로 환산하면 꽤 넓어서, 사람이 눈에 띄게 옆에 있어도 정지 구간에 들어갔다.
#   1/18 = 중심 **±17.8px @320** 으로 좁힌다 — 훨씬 예민하게 사람을 중앙에 붙인다.
#
#   ⚠️ **이제 화면의 3등분 가이드선이 제어 경계가 아니다.** 서버가 굽는 선은
#      `w//3`·`2w//3` 고정이라(`perception_server.draw_overlay`) 실제 정지 구간
#      (±w/18)보다 3배 넓게 그려진다. 화면만 보면 "가운데 칸인데 도네"로 보인다.
#      선을 같이 옮기려면 그쪽 오버레이를 고쳐야 한다(노트북 쪽, Pi 배포 불필요).
#
#   [2026-08-02 후] **1/18 → 1/24. "좀 더 가운데로" 요구로 한 번 더 좁혔다.**
#     ±17.8px → **±13.3px @320**. 화각 53° 기준 대략 ±3° → **±2.2°** 다.
#
#   ⚠️ **`ANGULAR_Z_MIN`(0.12) 과 맞물린다.** 구간을 벗어나는 순간 최소 0.12 rad/s 가
#      나가므로, 구간이 좁아질수록 **검출이 몇 px 떨 때마다 그 크기로 튄다.**
#      좌우로 깨작이면(도리도리) 순서는 이렇다:
#        ① 이 값을 1/18 로 되돌린다  ② 그래도 남으면 `ANGULAR_Z_MIN` 을 0.08 로 낮춘다
#        ③ 그래도 남으면 `ANGULAR_SMOOTHING` 을 0.2 로 낮춘다(더 느리게 붙는다)
#      ①이 가장 확실하다 — 원인이 "구간이 좁아 자주 벗어나는 것"이기 때문이다.
#   0 이면 구간을 끈다.
ANGLE_DEADZONE_FRAC = 1.0 / 24.0
# ⚠️ [2026-08-02] **라이다가 거꾸로 달려 있다.**
#   실측: 로봇 **뒤**를 손으로 막았는데 **앞으로 못 갔다** — 드라이버가 내보내는 0° 가
#   로봇의 뒤를 가리킨다는 뜻이다. 앞뒤와 좌우가 **동시에** 뒤집힌다(180° 회전).
#   `scan_provider.to_degree_indexed` 가 각도에 180 을 더해 좌표계를 바로잡는다 —
#   섹터를 나누기 **전**에 고치므로 아크를 나중에 바꿔도 안 어긋난다.
#   ⚠️ 라이다를 바로 달면 이 값을 False 로 되돌려야 한다. 안 되돌리면 정확히
#      지금과 반대로 틀어진다(뒤를 앞으로 본다).
LIDAR_FLIP_180 = True
# [2026-08-02] **1.3 → 1.0. 히스테리시스를 껐다.**
#
#   1.3 은 "가운데 칸을 30% 더 벗어나야 비로소 돈다"였다. 320 폭에서 칸 경계는
#   53.3px 인데 실제 문턱은 69.3px 이라, 그 사이 16px 이 **사람이 가이드선 밖으로
#   명백히 나갔는데도 로봇이 안 도는** 구간이었다. 화면과 제어가 어긋나면 화면이
#   거짓말을 한다(실측 2026-08-02: e_cx=75.5 화면을 두고 "이게 가운데냐").
#
#   1.0 = 눈에 보이는 선이 곧 문턱이다. 히스테리시스가 막아 주던 "도리도리"는
#   아래 `ANGULAR_Z_MIN` 이 원인(정지 마찰) 쪽에서 막는다 — `pid.py` 주석 참고.
#   ⚠️ 이제 `pid.py` 가 이 값을 **읽지 않는다.** 남겨 둔 것은 옛 설정 파일이
#      들어와도 안 깨지게 하려는 것뿐이다. 되살리려면 pid.py 를 같이 고쳐야 한다.
ANGLE_RESUME_RATIO = 1.0
# [2026-08-02] 0.60 → 0.40 → **0.70. 사용자가 회전을 더 빠르게 요구했다.**
#   데드존이 ±17.8 로 좁아져 빼낸 뒤 오차가 최대 142 → `KP_ANGLE × 142 = 0.57`.
#   상한이 그보다 위에 있어야 큰 각도에서 잘리지 않는다.
ANGULAR_Z_MAX = 0.70          # rad/s
# [2026-08-02] **정지 마찰 보상 — 새로 넣은 것이다.**
#
#   바퀴는 어느 크기 아래로는 아예 안 돈다. 그보다 작은 명령은 도는 것도 서는 것도
#   아닌 상태다(실측: -0.009 rad/s 에 odom 이 ±0.013 으로 떨기만 했다). 데드존을
#   벗어나 "돌기로 정한" 순간부터는 실제로 도는 크기를 보장한다.
#
#   ⚠️ 데드존 **안**에서는 안 걸린다(`pid.py` 가 빼낸 뒤 오차로 판정한다) — 안 그러면
#      가운데 칸에서 영원히 도는 로봇이 된다.
#   ⚠️ 로봇마다 다르다. 안 돌면 올리고, 최소 회전이 거칠면 내린다.
ANGULAR_Z_MIN = 0.12          # rad/s — 0 이면 끔
ANGULAR_SMOOTHING = 0.3       # low-pass: 0=frozen, 1=no smoothing

# ---- LiDAR avoidance ----
MIN_DIST = 0.20               # front-arc slowdown threshold (m)
# [2026-07-28] **하드 스톱**. 이보다 가까우면 전진을 0 으로 만든다.
#
#   MIN_DIST 는 비례 감속만 한다 — `linear_x *= front / MIN_DIST` 라
#   0.10m 에서 50%, 0.05m 에서 25% 로 줄 뿐 **0 이 되는 건 거리 0 에서다.**
#   관성까지 더하면 그대로 부딪힌다(2026-07-28 "그냥 들이박네").
#   비례 감속은 부드러움을 위해 남기고, 그 아래에 끊는 선을 하나 둔다.
#
#   ⚠️ 스캔이 **아예 없을 때**는 전진만 막는다. 후진은 살려 둔다 — 못 보는 것은
#      앞이고, 이미 붙어 있을 때 빠져나올 수단까지 없애면 안 된다.
#      뒤가 **보이는데 가까운** 경우는 아래 BACK_STOP_DIST 가 따로 막는다.
#
#   [2026-08-02] **0.25 → 0.09. 사용자 지정값이다.**
#
#   ⚠️ **안전 여유가 크게 줄었다.** 25cm 는 2026-07-28 "그냥 들이박네" 뒤에 넣은
#      값이다. 9cm 에서 명령을 0 으로 만들어도 **관성 때문에 그 뒤로 더 간다** —
#      실제 정지 거리는 9cm 보다 짧아질 수 없다. 이게 성립하는 근거는 두 가지다:
#        · `MIN_DIST`(0.20) 비례 감속이 20cm 부터 이미 속도를 깎고 있다
#        · `LINEAR_X_MAX` 를 0.06 m/s 로 낮췄다(예전 0.12 의 절반)
#      둘 중 하나라도 되돌리면 이 값도 같이 올려야 한다.
#
#   ⚠️ **라이다 최소 측정거리에 닿는다.** sllidar C1 은 사양상 5cm 부터고,
#      `lidar_avoidance.arc()` 가 `> 0.05` 만 유효 표본으로 친다. 9cm 는 그 바로
#      위라 반사가 약한 물체(검은 옷·유리)는 아예 안 잡힐 수 있다. 안 서면
#      "임계가 높아서"가 아니라 **점이 안 들어와서**일 가능성을 먼저 본다.
STOP_DIST = 0.09              # front-arc hard stop (m). 0 이면 끔
# [2026-08-02] **후방 하드 스톱을 앞과 분리했다.**
#   예전엔 `STOP_DIST` 하나를 앞뒤에 같이 썼다. 사용자가 앞 9cm · 뒤 8cm 로
#   따로 지정해 값을 갈랐다. 없으면(None) `STOP_DIST` 를 그대로 쓴다.
#   뒤가 더 빡빡한 이유: 로봇이 뒤를 보는 아크가 ±15° 로 좁아 옆으로 비낀
#   장애물을 놓치기 쉽다. bbox 가 커져 후진할 때 이 값이 유일한 방어선이다.
#   [2026-08-02 후] **0.08 → 0.12. 사용자 지정.** 뒤는 카메라가 없어서
#     사람이 눈으로 확인할 수단도 없다 — 앞보다 여유를 더 둔다.
BACK_STOP_DIST = 0.12         # back-arc hard stop (m). 0 이면 끔
# [2026-07-31] **후방 아크 — 없던 것이다.**
#
#   회피가 보던 각도는 전방 ±15° 와 측면 16~70°(좌우) 뿐, 합쳐 141° 였다.
#   **71~289° 는 아무도 안 봤다.** 그래서 후진에는 막을 근거 자체가 없었고,
#   config 주석도 "후진은 살려 둔다"로 그 사실을 굳혀 놓고 있었다.
#
#   ⚠️ 그 전제가 지금 깨졌다. `TARGET_SIZE` 가 360 이던 시절에는 후진이 "영영
#      안 걸린다"고 `pid.py` 주석이 적어 두었는데(도달 불가능한 목표라 오차가 늘
#      양수), 280 으로 내리면서 **후진이 실제로 나온다.** 뒤를 못 보는 채로.
#
#   패널은 이미 뒤쪽 거리를 **보여주고 있었다** — `aba_ai_service` 쪽
#   `lidar_avoid.sectors8` 이 8방향을 다 계산해 LIDR 로 보낸다. 사람은 보는데
#   로봇만 못 쓰는 상태였다. 라이다는 처음부터 360° 다.
#
#   전방과 같은 폭으로 둔다. 다른 값을 쓸 근거(실측)가 없고, 좌우 대칭이 아니면
#   "앞은 서는데 뒤는 안 선다"가 다시 생긴다. 0 이면 끈다(예전 동작).
BACK_ARC_DEG = 22             # +/- degrees around 180 (rear). 0 이면 후방 감시 끔
AVOID_DIST = 0.40             # side-arc shy-away 시작 (m) — 여기부터 비례로 부드럽게 비킨다
# [2026-08-02] **측면 하드 차단 — 벽에 안 붙는 것이 추종보다 우선이다.**
#
#   이 거리 안에 벽이 있으면:
#     · 추종이 요구한 각속도를 **덮어쓴다**(더하지 않는다) → 그쪽으로 도는 일 자체가 없다
#     · 그쪽으로 가는 **전진도 막는다** → 벽에 붙은 채 밀면 긁는다
#   벗어나면 위 `AVOID_DIST` 비례 구간을 거쳐 **원래 추종 각속도로 돌아온다.**
#
#   참조 구현(`arte_libi_perception/.../lidar_avoid.py`)의 `SIDE_AVOID`(0.10)과 같은 뜻.
#   0 이면 끈다.
SIDE_BLOCK_DIST = 0.10        # m
#: 하드 차단 구간에서 반대편으로 내보내는 각속도. 참조 구현의 `SIDE_DRIFT`.
#  추종 명령을 덮어쓰는 값이라 **크면 안 된다** — 벽에서 떨어지기만 하면 된다.
SIDE_DRIFT = 0.12             # rad/s
AVOID_KP = 0.50
# [2026-08-02] 15 → **22.** 참조 구현(`arte_libi_perception/.../lidar_avoid.py`)의
#   8섹터 분할과 맞췄다(정면 ±22.5°). 좌우는 이제 "전방·후방을 뺀 나머지 전부"라
#   이 값이 곧 좌우 섹터의 시작점이기도 하다 — 360° 에 빈 각도가 없다.
FRONT_ARC_DEG = 22            # +/- degrees around 0 (front)
# [2026-07-31] 20 → 16. **어느 아크에도 안 들어가는 각도가 있었다.**
#   전방은 345~359°·0~15°, 측면은 20~70°·290~340° 였다 —
#   **16~19° 와 341~344° 가 비어** 그 방향의 장애물은 감속에도 조향에도 안 잡혔다.
#   16 으로 붙이면 전방 아크와 공백도 중복도 없다.
#
# [2026-08-02] **끝을 71 → 111 로 넓혔다. 정옆(90°)이 사각지대였다.**
#
#   좌 16~70° / 우 290~344° 만 보고 있었다. 즉 **로봇 바로 옆에 벽이나 사람이
#   붙어도 회피가 안 걸렸다** — 사용자 보고: "좌우에서 오는데 반대편으로 안 튼다".
#   90° 가 어느 아크에도 없었던 것이 원인이다(codex 검증 2026-08-02).
#
#   111 로 두면 좌 16~110° / 우 250~344° 가 되어 정옆이 넉넉히 들어온다.
#   후방 아크(165~195°)와 겹치지 않는다 — 겹치면 후진 정지와 측면 조향이
#   같은 점을 두고 다툰다.
#
#   ⚠️ 남는 사각: 111~164° 와 196~249°(비스듬한 뒤). 그쪽은 조향으로 피할 수
#      있는 방향이 아니라 일부러 안 넣었다 — 뒤는 `BACK_ARC_DEG` 가 정지로 막는다.
#   ⚠️ 넓힐수록 좁은 복도에서 더 자주 조향이 걸린다. 벽 사이에서 흔들리면 줄인다.
SIDE_ARC = (16, 111)         # degrees range for a side arc (start, stop-exclusive)

# ---- LiDAR 건강성 (fail-safe) ----
# [2026-07-31] 회피가 **조용히 꺼지는** 두 경우를 막는다.
#
#   예전에는 스캔이 비어 있으면 `apply_avoidance` 가 속도를 그대로 통과시켰다.
#   즉 라이다가 죽거나 아직 안 올라왔으면 **회피 없이 전속 전진**이었고, 그 사실이
#   어디에도 안 드러났다. 앞을 못 보면 앞으로 가지 않는 것이 맞다.
#
#   ⚠️ **전진만** 막는다. 후진과 회전은 살린다 — 못 보는 것은 앞이고, 이미 붙어 있을 때
#      빠져나올 수단까지 없애면 안 된다. (STOP_DIST 와 같은 규칙이다)
#
#   ⚠️ /scan 이 없는 곳에 추종 노드를 띄우면 이 값 때문에 **전진이 아예 안 된다.**
#      그건 의도다. 라이다 없이 굴릴 거면 0 으로 꺼라.
SCAN_MAX_AGE_SEC = 1.0        # 이보다 오래된 스캔은 없는 것으로 본다. 0 이면 검사 끔
# 전방 아크(±FRONT_ARC_DEG = 31칸)에서 이만큼도 못 채우면 "앞을 못 본다"로 본다.
# sllidar C1 은 스캔당 1000+ 포인트라 정상이면 31칸이 거의 다 찬다. 0 이면 검사 끔.
FRONT_MIN_SAMPLES = 5
# [2026-08-02] **후방에도 같은 검사를 넣었다 — 전방에만 있었다.**
#
#   `arc()` 는 유효 표본이 하나도 없으면 10.0m(=멀다)를 돌려준다. 그래서 후방
#   아크가 통째로 비어도 "뒤가 뻥 뚫렸다"와 구별되지 않았고, `BACK_STOP_DIST`
#   하드 스톱이 **조용히 안 걸렸다**(codex 검증 2026-08-02).
#
#   비는 이유는 실제로 있다: 유리·검은 옷·라이다 최소거리(5cm) 미만이면 반사가
#   사라진다. 특히 **뒤에 아주 가까이 붙었을 때** 표본이 없어지는데, 그때가 바로
#   후진을 막아야 하는 순간이다.
#
#   ⚠️ **스캔이 통째로 없는 경우와는 다르다.** 그쪽은 여전히 후진을 살린다
#      (`apply_avoidance` 의 `if not scan`) — 라이다가 죽었다고 탈출 수단까지
#      뺏으면 앞에 낀 로봇을 못 꺼낸다. 여기서 막는 것은 "스캔은 오는데 뒤만
#      안 보이는" 경우다. 그 상태에서도 **회전은 살아 있으므로** 빠져나갈 길은 남는다.
#   0 이면 검사 끔(예전 동작).
BACK_MIN_SAMPLES = 5

# ---- 안내(guide) 코스팅 ----
# [2026-08-02] **안내는 예측 bbox 를 이 시간만큼만 받는다. 사용자 지정 1.4초.**
#
#   예전엔 안내에서 예측을 통째로 거부했다(코스팅 0초). 그런데 안내는 로봇이 앞서고
#   요청자가 뒤따르는 구조라 **서가·기둥이 계속 사이를 지나** 잠깐 가려지는 일이
#   추종보다 오히려 잦다. 0 이면 그때마다 즉시 "놓쳤다"가 된다.
#
#   추종(`COAST_LIMIT` 21프레임 = 1.40초 @15fps)과 **같은 값**으로 맞춘다. 한때 0.7초로
#   더 짧게 두려 했지만, 두 역할에서 "얼마나 가려짐을 참는가"가 다를 근거가 실측으로
#   없었다. 값이 다르면 어느 쪽이 왜 그런지 나중에 아무도 설명 못 한다.
#
#   ⚠️ 프레임이 아니라 **초**다 — 제어 루프(20Hz)와 검출(15fps)의 주기가 달라
#      같은 예측 프레임을 여러 번 보기 때문에 프레임 수로 세면 짧아진다.
#      그래서 추종의 `COAST_LIMIT`(프레임)과 단위가 다르다. 둘을 같이 조정할 것.
#   0 이면 예전 동작(예측 통째 거부).
GUIDE_COAST_SEC = 1.4

# ---- miss / search ----
N_MISS_FRAMES = 40            # consecutive None before TRACKING -> SEARCHING
# 탐색(회복) 타임라인.
#   LkdPeek ~4.5s(마지막 방향으로 90°, 추종 전용)
#   → HoldFront 5s(앞,정지) → HoldBack 5s(뒤,정지)
#   → SweepFront(좌우로 훑고 원위치) → SweepBack(뒤캠으로 같은 왕복) → GiveUp
# 어느 구간에 있든 앞캠에서 보이면 즉시 재개, 뒤캠에서 잡히면 즉시 180° 회전한다
# (recovery_bt 의 Selector 우선순위). 구간을 다 돌 필요가 없다.
SEARCH_HOLD_SEC = 5.0         # 앞/뒤 각각 서서 보는 시간
SEARCH_SWEEP_ANGLE = 3.14159  # 훑는 전체 폭(rad). 중앙 기준 ±90°
ANGULAR_Z_SWEEP = 0.55        # rad/s — 훑기 각속도 (ANGULAR_Z_MAX 아래로 유지)
ANGULAR_Z_SEARCH = 0.35       # rad/s — 180° 정렬 회전(AlignHeading) 각속도
SEARCH_TURN_ANGLE = 3.14159   # AlignHeading 이 도는 각도 (radians)
# [2026-08-01] **LKD 90° peek — 되살린 것이다.**
#
#   `aba_ai_service/follower_BT/recovery.py` 의 `DrivePolicy` 에 처음부터 있었다:
#       FOLLOWING --owner lost--> PEEK --~90 deg turned, still lost--> SEARCHING
#       PEEK_ANGLE = math.radians(90)   # turn ~90 deg toward the last direction
#   그런데 그 상태기계의 각속도는 `cmd_sink` 로만 나가고, 그 경로(`--drive-host`)는
#   2026-07-28 에 폐기됐다. **화면에는 계속 "PEEK" 이 뜨는데 바퀴는 안 돌았다** —
#   실제 주행을 하는 이쪽 트리에는 이 구간이 아예 없었기 때문이다.
#
#   서서 기다리는 유예는 이미 α-β coast(`COAST_LIMIT` 30프레임 ≈ 2s)가 준다. 그게
#   만료되고 `N_MISS_FRAMES` 까지 지나 탐색에 들어온 시점이면, 사람은 "잠깐 가렸다"가
#   아니라 **어딘가로 나간 것**이다. 그래서 훑기 전에 그 방향부터 본다.
#
#   ⚠️ **추종 전용이다.** 길잡이는 돌지 않는다 — 목적지 방향과 사람 방향이 겹치면
#      회전 → 경로 재계획 → 재포착 → 재회전으로 무한 진동한다(`AlignHeading` 주석).
#   ⚠️ 0 으로 두면 이 구간이 사라진다(길이 0 인 구간은 트리에서 빠진다).
SEARCH_PEEK_ANGLE = 1.5708    # LKD 방향으로 꺾는 각도(rad). 0 이면 끔

# ---- loop ----
TICK_HZ = 20.0
FRAME_DT = 0.05               # nominal seconds per tick


# ---- 세션 ----
# 이 시간 갱신이 없으면 세션을 닫는다. 패널이 죽어 stop 이 안 오는 경우 대비.
SESSION_LEASE_SEC = 60.0

#: GUIDE 감시 세션을 **주인 없는 것**으로 보기까지의 시간(초).
#
# GUIDE 세션은 lease 면제다(`session.py` `expired` — BT 가 열고 BT 가 닫는다).
# 그런데 `follow_node` 가 GUIDE 세션 중 패널의 `watch` 를 거절하게 되면서, BT 가
# 죽어 세션이 고아가 되면 **패널이 길잡이 등록을 영영 못 여는** 잠금이 생겼다.
#
# `GuideExec` 이 `WATCH_RENEW_SEC`(15초)마다 `guide_watch` 를 재발행하므로, 그보다
# 넉넉히 긴 이 시간 동안 아무 소식이 없으면 주인이 없는 것이다.
#
# ⚠️ **`WATCH_RENEW_SEC` 보다 충분히 커야 한다.** 비슷하면 BT tick 이 잠깐 밀린
#    것만으로 살아 있는 안내가 고아로 잡혀, 패널이 안내 중인 로봇의 감시를 뺏는다.
#    지금은 15초 갱신 : 45초 판정 = 3배.
GUIDE_ORPHAN_SEC = 45.0
# camera_select 재발행 주기(Hz). 송출기 쪽 만료(CAMERA_SELECT_EXPIRY_SEC)보다
# **촘촘해야** 한다 — 안 그러면 정상 세션 중에 카메라가 깜빡인다.
CAMERA_SELECT_HZ = 2.0
# 송출기가 이 시간 갱신을 못 받으면 스스로 none 으로 떨어진다(참고용 — 실제 값은
# camera_sender 쪽 --select-expiry 다). 여기 적어두는 이유는 둘의 관계가 규칙이기 때문.
CAMERA_SELECT_EXPIRY_SEC = 3.0

# ---- transport ----
DETECTION_TCP_HOST = '0.0.0.0'
DETECTION_TCP_PORT = 6000
SCAN_TOPIC = '/scan'
# ⚠️ `/cmd_vel` 직접 발행 금지. twist_mux 가 중재하는 입력으로 낸다
#    (pinky_bringup/config/twist_mux.yaml — follow, priority 100).
#    직접 내면 중재를 우회해 nav2·수동조작과 마지막 메시지 싸움이 된다.
CMD_VEL_TOPIC = '/cmd_vel_follow'
CAMERA_SELECT_TOPIC = '/libi/camera_select'
# 지금 어떤 세션이 도는가 — "follow" / "guide" / "watch" / "none".
#
# AI 서버는 원래 앞·뒤 카메라만 알았고 **역할은 몰랐다.** 그래서 자세(pose) 추정이
# 길잡이 중에도 그대로 돌고 골격이 패널 영상에 구워져 나갔다(`perception_server`
# 가 JPEG 에 직접 그린다 — 패널은 끌 수 없다). 자세 게이트는 사람에게 다가가는
# **추종 전용** 안전 규칙이라(`control_loop._is_guide` 가 이미 값을 버린다) 길잡이에서는
# 계산도 표시도 할 이유가 없다.
#
# ⚠️ 카메라로 대신 판정하면 안 된다. 길잡이 등록은 **앞캠**에서 하고, 추종 회복은
#    **뒷캠**을 잠깐 본다(`request_camera`) — 역할과 카메라는 1:1 이 아니다.
PERCEPTION_ROLE_TOPIC = '/libi/perception_role'
REQUESTER_VISIBLE_TOPIC = '/libi/requester_visible'
REQUESTER_AREA_TOPIC = '/libi/requester_area'
# 화면 안 **가장 큰 사람**(owner 매칭과 무관, 통행인도 포함) 의 sqrt(area) px.
# 주행 중 사람 차단 판정(`libi_modes` 의 `PersonBlockGuard`)이 구독한다.
# 검출이 없으면 0.0 을 낸다 — "모른다"를 안 내는 게 아니라 0 을 낸다(구독자가
# 없음으로 해석). `Detection.front_person_size` 머리말 참고.
FRONT_PERSON_SIZE_TOPIC = '/libi/front_person_size'
# ⚠️ [2026-08-02] **회복 BT 가 포기했다는 것을 안내 쪽에 알리는 통로.**
#
#   예전에는 `GuideExec` 이 `guide_lost_timeout_sec`(시계)로 안내를 끝냈다. 회복
#   트리가 실제로 언제 끝나는지 아무도 안 알려줬기 때문이다 — 감시 세션은
#   `_active_id` 를 안 세우므로 `FollowSession.poll()` 의 실패가 `tick()` 의
#   `if self._active_id is None: return` 에 걸려 **결과가 발행되지 않았다.**
#   그래서 "회복이 끝났다" 와 "N 초가 지났다" 가 다른 사건인데 하나로 뭉개졌고,
#   트리 타임라인을 바꿀 때마다 params 숫자를 손으로 다시 맞춰야 했다.
#
#   ⚠️ `/fleet_cmd_result` 를 재사용하지 않는 이유: `FleetCmdDriver.poll()` 은 결과를
#      읽으면 `_pending_id` 를 지우고, 그러면 뒤따르는 `stop()` 이 조용히 아무것도
#      안 보내 **GUIDE 세션이 고아로 남는다**(그러면 패널이 등록 화면을 다시는 못
#      연다 — `follow_node._guide_orphaned` 주석의 그 잠금이다).
#      가시성과 같은 결의 Bool 을 하나 더 두는 쪽이 훨씬 싸다.
GUIDE_SEARCH_FAILED_TOPIC = '/libi/guide_search_failed'
