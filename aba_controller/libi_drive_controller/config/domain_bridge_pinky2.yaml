# 로봇2(Pinky-2, 192.168.0.42, ROS_DOMAIN_ID=89) ↔ 서버 도메인 86 중계.
# 텔레메트리/상태(89→86, /pinky2/* 개명)와 명령(86→89, reversed)을 함께 담당한다.
# 로봇은 무수정 — 서버에서만 실행: ros2 run domain_bridge domain_bridge <이 파일>
name: bridge_pinky2
from_domain: 89
to_domain: 86
topics:
  amcl_pose:
    type: geometry_msgs/msg/PoseWithCovarianceStamped
    remap: /pinky2/amcl_pose
  # 라이다 스캔(relative_move 안전정지용). sensor QoS(BEST_EFFORT) 자동 매칭.
  scan:
    type: sensor_msgs/msg/LaserScan
    remap: /pinky2/scan
  map:
    type: nav_msgs/msg/OccupancyGrid
    remap: /pinky2/map
  plan:
    type: nav_msgs/msg/Path
    remap: /pinky2/plan
  battery/percent:
    type: std_msgs/msg/Float32
    remap: /pinky2/battery/percent
  battery/voltage:
    type: std_msgs/msg/Float32
    remap: /pinky2/battery/voltage
  # ── fleet link (robot_agent app/core/fleet_link.py, 2026-07-08) ──
  fleet_status:
    type: std_msgs/msg/String
    remap: /pinky2/fleet_status
  fleet_costmaps:
    type: std_msgs/msg/String
    remap: /pinky2/fleet_costmaps
  fleet_cmd_result:
    type: std_msgs/msg/String
    remap: /pinky2/fleet_cmd_result
  # 역방향 명령: 86의 /pinky2/fleet_cmd 구독 → 89에 /fleet_cmd 발행
  pinky2/fleet_cmd:
    type: std_msgs/msg/String
    reversed: True
    remap: /fleet_cmd
  # ── [2026-07-30] 역방향 속도 명령 — 이것 때문에 버그가 너무 많아서 뺐다 ──────────
  # 근거·되살리는 법은 domain_bridge_pinky3.yaml 의 같은 블록에 적어 뒀다.
  # 요약: remap 대상 `/cmd_vel` 이 **twist_mux 의 출력**이라 중재 밖에서 바퀴로 직행한다.
  # 우선순위·잠금·비상정지가 전부 안 먹는다. 되살리려면 twist_mux 입력 토픽으로 바꿔라.
  #
  # pinky2/cmd_vel:
  #   type: geometry_msgs/msg/Twist
  #   reversed: True
  #   remap: /cmd_vel
