# 2026-07-20 작업 내역 — FastAPI/ROS2 역할 분리 리팩토링

## ROS2 통신 방식 정리

| 방식 | 방향 | 특징 | 사용처 |
|------|------|------|--------|
| **토픽 (Topic)** | 발행자 → 구독자 (1:N) | 연속 데이터, 단방향 브로드캐스트 | 센서, cmd_vel, SLAM 맵 |
| **서비스 (Service)** | 클라이언트 → 서버 (요청/응답) | 단발 제어, 결과 확인 가능 | LED, LCD, 부저, SLAM reset |
| **액션 (Action)** | 클라이언트 → 서버 (요청/진행/완료) | 장시간 작업, 중간 피드백 | NavigateToPose |

---

## 최종 역할 분리 결과

**FastAPI 유지 (HTTP 필수):**
- 카메라 MJPEG 스트리밍 — 브라우저는 ROS2 토픽 직접 구독 불가, HTTP 스트림 필요
- LCD 이미지/폰트 파일 업로드 — 바이너리 파일 전송은 HTTP가 효율적
- WebSocket 조이스틱 — 브라우저 연결 게이트웨이 (내부는 `/cmd_vel` 발행)

**ROS2 전환 완료:**

| 기능 | 토픽/서비스 | 방식 | 패키지 |
|------|------------|------|--------|
| 모터 제어 | `/cmd_vel` | 토픽 발행 | pinky_bringup |
| LED 색상/픽셀 | `/set_led` | 서비스 | pinky_bringup |
| LED 밝기 | `/set_brightness` | 서비스 | pinky_bringup |
| LCD 표정 (GIF) | `/set_emotion` | 서비스 | pinky_bringup |
| LCD 텍스트 | `/lcd_text` | 서비스 | **pinky_lcd_server** (신규) |
| LCD 중지 | `/lcd_stop` | 서비스 | **pinky_lcd_server** (신규) |
| 부저 단음 | `/play_buzzer` | 서비스 | **pinky_buzzer** (신규) |
| 부저 멜로디 | `/play_melody` | 서비스 | **pinky_buzzer** (신규) |
| 부저 중지 | `/stop_buzzer` | 서비스 | **pinky_buzzer** (신규) |
| 초음파 센서 | `/us_sensor/range` | 토픽 구독 | pinky_sensor_adc |
| IR 센서 | `/ir_sensor/range` | 토픽 구독 | pinky_sensor_adc |
| Nav2 항법 | NavigateToPose 액션 | 액션 | nav2_bringup |
| Nav 명령 | `/fleet_cmd` 토픽 | 토픽 | fleet_link.py |

---

## 1차 작업 — nav_router 제거 + 센서/LED/표정 ROS2 전환

### 변경 파일

#### `robot_agent/app/routers/driving.py`
- **1272줄 → ~530줄**
- 제거: 모터 데몬(`_motor_proc`, `_motor_send`, `_motor_close`)
- 제거: 센서 데몬(`_sensor_proc`, cached_ultrasonic/IR 함수)
- 제거: `nav_router` 전체 (~700줄) — `/api/state`, `/api/goal`, `/api/goto`, `/api/locations/*`, `/api/mission/*`, `/api/slam/*`, `/api/schedule/*`
- 제거: 자율탐색 (미사용 확인)
- 변경: `/ws/drive` — ROS2 전용, 모터 데몬 폴백 제거
- 변경: `/led/*` → `ros_bridge.call_set_led()` / `call_set_brightness()`
- 변경: `/lcd/emotion` → `ros_bridge.call_set_emotion()`
- 변경: `/lcd/text` → `ros_bridge.call_lcd_text()`
- 변경: `/lcd/stop` → `ros_bridge.call_lcd_stop()`
- 변경: `/buzzer` → `ros_bridge.call_play_buzzer()`
- 변경: `/buzzer/melody/play` → `ros_bridge.call_play_melody()`
- 변경: `/buzzer/melody/stop` → `ros_bridge.call_stop_buzzer()`
- 변경: `/sensor/ultrasonic`, `/sensor/ir` → `ros_bridge.get_topic()`
- 변경: `/motor/move`, `/motor/stop` → `ros_bridge.publish_cmd_vel()`

#### `robot_agent/app/core/ros_bridge.py`
- `_state` 딕셔너리에 `us_range`, `ir_range` 키 추가
- 구독 추가: `/us_sensor/range` (Range), `/ir_sensor/range` (UInt16MultiArray)
- 서비스 클라이언트 추가: `/set_led`, `/set_brightness`, `/set_emotion`, `/play_buzzer`, `/play_melody`, `/stop_buzzer`, `/lcd_text`, `/lcd_stop`
- 모듈 레벨 call_* 함수 11개 추가

#### `robot_agent/app/core/server.py`
- `api.include_router(driving.nav_router, ...)` 제거

#### `robot_agent/app/schemas/driving.py`
- Nav2 전용 스키마 6종 제거

#### `aba_fms_service/backend/app/fleet_coordinator.py`
- HTTP 폴백 3곳 제거, `_fetch_json()` 제거
- 미사용 import 제거: `socket`, `urllib.error`, `urllib.request`, `STATE_TIMEOUT`

#### `pinky_bringup/launch/bringup_robot.launch.xml`
- `pinky_sensor_adc main_node` 추가 (기존 누락)
- `pinky_buzzer buzzer_node` 추가
- `pinky_lcd_server lcd_node` 추가

---

## 2차 작업 — 부저/LCD 텍스트 ROS2 패키지 신규 생성

### 신규: `pinky_interfaces/srv/` 4종 추가

| 파일 | 요청 필드 | 응답 |
|------|----------|------|
| `PlayBuzzer.srv` | preset, count, freq, duration | success, message |
| `PlayMelody.srv` | melody | success, message |
| `StopBuzzer.srv` | (없음) | success, message |
| `LcdText.srv` | text, font_path, font_size, color, bg_color, align, scroll, scroll_speed | success, message |

`/lcd_stop` 은 표준 `std_srvs/srv/Trigger` 사용.

### 신규: `pinky_buzzer` 패키지

```
pinky_buzzer/
├── pinky_buzzer/buzzer_node.py  ← BuzzerNode 클래스
├── setup.py / setup.cfg / package.xml
└── resource/pinky_buzzer
```

- `/play_buzzer` — `pinkylib.Buzzer` 직접 호출, 동기 beep
- `/play_melody` — 백그라운드 threading.Thread로 실행, 즉시 응답
- `/stop_buzzer` — threading.Event로 멜로디 루프 중단

### 신규: `pinky_lcd_server` 패키지

```
pinky_lcd_server/
├── pinky_lcd_server/lcd_node.py  ← LcdNode 클래스
├── setup.py / setup.cfg / package.xml
└── resource/pinky_lcd_server
```

- `/lcd_text` — threading.Thread로 정적/스크롤 루프 실행, `pinky_lcd.LCD` 직접 호출
- `/lcd_stop` (std_srvs/Trigger) — 스레드 중지 + `/tmp/pinky_lcd_ctrl.pid` 프로세스 종료 (emotion GIF 공존 처리)

---

## 메모

- `pinky_sensor_adc` 는 `aarch64`(ARM64) 전용 빌드
- IR ADC 임계값(`_IR_THRESHOLD = 2048`) — 실물 센서 테스트 후 조정 필요
- 초음파 거리: 토픽은 m 단위, API 응답은 cm 변환 (`× 100`)
- `/set_emotion`(GIF)과 `/lcd_text` 동시 사용 시 마지막 호출이 화면 차지 — 동시 사용 지양
- 카메라는 ROS2 전환 비권장 (직렬화 오버헤드로 스트리밍 지연 발생)
