# 2026-07-20 작업 내역 — FastAPI/ROS2 역할 분리 리팩토링

## 개요

robot_agent(FastAPI)와 ROS2 패키지 간의 역할 경계를 명확하게 재정의했다.

**FastAPI 유지:**
- 카메라 스트리밍
- 부저 / 멜로디
- LCD 텍스트 · 이미지 · 폰트 업로드
- WebSocket 조이스틱 (`/ws/drive` → `/cmd_vel` 발행)

**ROS2 토픽/서비스로 전환:**
- 모터 직접 제어 → `/cmd_vel` 토픽
- LED 색상 · 픽셀 · 밝기 → `/set_led`, `/set_brightness` 서비스
- LCD 표정 → `/set_emotion` 서비스
- 초음파 · IR 센서 → `/us_sensor/range`, `/ir_sensor/range` 구독
- Nav2 명령 · SLAM · 위치 · 미션 → `fleet_link.py` (`/fleet_cmd` 토픽) 로 일원화

---

## 변경 파일

### 1. `aba_controller/.../robot_agent/app/routers/driving.py`
- **1272줄 → 580줄 (-692줄)**
- 제거: 모터 데몬(`_motor_proc`, `_motor_send`, `_motor_close`)
- 제거: 센서 데몬(`_sensor_proc`, `_sensor_reader_task`, cached_ultrasonic/IR 함수)
- 제거: `_exploration_loop()` (구형 초음파/모터 데몬 기반 자율탐색)
- 제거: `nav_router` 전체 (~700줄) — `/api/state`, `/api/goal`, `/api/goto`, `/api/locations/*`, `/api/mission/*`, `/api/slam/*`, `/api/schedule/*`
- 제거: 자율탐색(라이다 기반) — 미사용으로 확인
- 변경: `/ws/drive` — ROS2 전용, 모터 데몬 폴백 제거
- 변경: `/led/*` — subprocess → `ros_bridge.call_set_led()` / `call_set_brightness()`
- 변경: `/lcd/emotion` — subprocess → `ros_bridge.call_set_emotion()`
- 변경: `/sensor/ultrasonic`, `/sensor/ir` — subprocess 데몬 → `ros_bridge.get_topic()`
- 변경: `/motor/move`, `/motor/stop` — subprocess → `ros_bridge.publish_cmd_vel()`
- 제거: 미사용 import (`io`, `zipfile`, `math`, `fastapi.responses.Response`)
- 제거: Nav2 전용 스키마 import 6종

### 2. `aba_controller/.../robot_agent/app/core/ros_bridge.py`
- `_state` 딕셔너리에 `us_range`, `ir_range` 키 추가
- import 추가: `sensor_msgs/Range`, `std_msgs/UInt16MultiArray`, `pinky_interfaces/srv/{SetLed,SetBrightness,Emotion}`
- `BridgeNode.__init__` 에 구독 추가:
  - `/us_sensor/range` (sensor_msgs/Range)
  - `/ir_sensor/range` (std_msgs/UInt16MultiArray)
- `BridgeNode.__init__` 에 서비스 클라이언트 추가:
  - `/set_led` (SetLed), `/set_brightness` (SetBrightness), `/set_emotion` (Emotion)
- `BridgeNode` 메서드 추가:
  - `_on_us_range()` — 거리(m) 수신, cm 변환 후 캐시
  - `_on_ir_range()` — ADC(0~4095) 수신, 임계값(2048) 이진화, 장애물 여부 판정
  - `call_set_led()`, `call_set_brightness()`, `call_set_emotion()` — fire-and-forget
- 모듈 레벨 함수 추가: `call_set_led()`, `call_set_brightness()`, `call_set_emotion()`

### 3. `aba_controller/.../robot_agent/app/core/server.py`
- `api.include_router(driving.nav_router, tags=["nav"])` 제거

### 4. `aba_controller/.../robot_agent/app/schemas/driving.py`
- Nav2 전용 스키마 제거: `GoalRequest`, `LocationSetRequest`, `NameRequest`, `MissionStartRequest`, `ScheduleStartRequest`, `SaveMapRequest`
- 유지: `MoveRequest`, `RotateRequest`, `DriveState`

### 5. `aba_fms_service/backend/app/fleet_coordinator.py`
- `_robot_state()` — `_fetch_json` HTTP 폴백 제거, ROS 텔레메트리 캐시만 사용
- `_stop_robot()` — `_fetch_json` HTTP 폴백 제거
- `_move_robot()` — `_fetch_json` HTTP 폴백 제거
- `_fetch_json()` 함수 제거
- 미사용 import 제거: `socket`, `urllib.error`, `urllib.request`
- 미사용 상수 제거: `STATE_TIMEOUT`

### 6. `aba_controller/.../ros_ws/src/pinky_pro/pinky_bringup/launch/bringup_robot.launch.xml`
- `pinky_sensor_adc main_node` 추가 (초음파/IR/배터리 ADC 퍼블리셔 — 기존 누락)

---

## 메모

- `pinky_sensor_adc` 는 `aarch64`(ARM64) 전용 빌드 (CMakeLists.txt `APPLE` 조건부 제외)
- IR ADC 임계값(`_IR_THRESHOLD = 2048`)은 실물 센서 테스트 후 조정 필요
- 초음파 거리 단위: 토픽은 m, API 응답은 cm 변환(`× 100`)
- `fleet_link.py`가 Nav2 명령 전체를 처리하므로 `nav_router` HTTP 폴백 없이도 정상 동작
