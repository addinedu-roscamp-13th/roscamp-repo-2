# CONF_MIN 스윕 — yolo11n-pose

⚠️ `pose_bench.py` 의 `--conf-min` 은 원래 지표·라벨 게이트만 바꿨다. 이번에
`posture.CONF_MIN` 을 실제로 덮어쓰도록 고친 뒤 잰 값이다. 그 전 숫자와 비교하면 안 된다.

## front (`pi_20260731_122344.mp4`, rotate=180)

| conf_min | torso4_pass | jitter_torso4 | accuracy | undecided_st | fatal_frames |
|---|---|---|---|---|---|
| 0.50 (현재) | 0.687 | 0.185 | 0.197 | 0.700 | 16 |
| 0.45 | 0.707 | 0.193 | 0.202 | 0.694 | 16 |
| 0.40 | 0.715 | 0.193 | 0.202 | 0.688 | 16 |
| 0.30 | 0.720 | 0.207 | 0.202 | 0.688 | 16 |
| 0.20 | 0.741 | 0.212 | 0.202 | 0.681 | 16 |

## back (`usb_20260731_122555.mp4`, rotate=0)

| conf_min | torso4_pass | jitter_torso4 | accuracy | undecided_st | fatal_frames |
|---|---|---|---|---|---|
| 0.50 (현재) | 0.422 | 0.143 | 0.263 | 0.646 | 16 |
| 0.45 | 0.436 | 0.156 | 0.269 | 0.642 | 16 |
| 0.40 | 0.453 | 0.156 | 0.269 | 0.635 | 16 |
| 0.30 | 0.480 | 0.202 | 0.269 | 0.627 | 16 |
| 0.20 | 0.529 | 0.240 | 0.278 | 0.613 | 16 |

## 판정

⚠️ **`torso4_pass` 는 진단 입력 하나이지, 그 자체로 배포 기준이 아니다**
(codex adversarial review 2026-08-01 — 원래 이 계획이 쓰던 "0.95 목표선"은 근거를
못 대는 발명된 숫자였다. torso4_pass는 프레임당 통과율이고, 정지 채터링은 시간축에서
`Unknown` 이 **연속으로 얼마나 이어지는지**로 결정된다. 5%의 프레임당 Unknown율도
그게 뭉쳐서 나오면(예: 회전 중 1초씩 몰림) 용납 못 할 채터링을 만들 수 있다 —
전체 평균만으로는 그 뭉침을 못 본다).

이 Task 가 실제로 답할 수 있는 것과 없는 것:

- 답할 수 있음: `conf_min` 을 낮추면 `torso4_pass` 가 얼마나 오르고 `jitter_torso4`·
  `accuracy` 는 얼마나 나빠지는가 (트레이드오프의 모양)
- 답할 수 없음: 그 트레이드오프가 "정지 채터링 없이 즉시 정지를 켜도 되는가"에
  대한 답 — 그러려면 실제 운영 조건에서 분당 오탐 정지 횟수, `Unknown` 연속 길이의
  분포(특히 상위 백분위수), 위험 자세(누움)를 놓치는 지연시간을 각각 재는 **별도의
  운영 로깅 인프라**가 필요하고, 지금 이 저장소 어디에도 그런 로깅이 없다. 새
  인프라를 만드는 것은 이 측정 Task 의 범위를 넘는다.

- conf_min 을 낮췄을 때 torso4_pass·accuracy·jitter 가 어떻게 움직이는가(실측 표만):
  0.50→0.20 으로 내리면 `torso4_pass` 는 두 캠 다 꾸준히 오른다(front 0.687→0.741,
  +0.054p / back 0.422→0.529, +0.107p — 뒷캠이 절대적으로도 상대적으로도 이득이 더
  크다). 반대급부인 `jitter_torso4` 도 두 캠 다 꾸준히 나빠진다(front 0.185→0.212,
  back 0.143→0.240 — 뒷캠이 훨씬 더 나빠진다, +68% 상대증가). `accuracy` 는 이 표에서
  가장 둔감하다 — front 는 0.50→0.45 에서 0.197→0.202 로 한 번 오른 뒤 0.20 까지 완전히
  평평하고, back 은 0.50→0.45 에서 0.263→0.269 로 오른 뒤 0.40·0.30 에서 평평하다가
  0.20 에서 한 번 더 0.278 로 오른다 — 즉 accuracy 개선은 계단형이지 conf_min 을 내린
  만큼 선형으로 따라오지 않는다. `undecided_st` 는 완만히만 줄어든다(front
  0.700→0.681, back 0.646→0.613). `fatal_frames` 는 두 캠 다 스윕 전 구간에서
  **16/16 그대로**, 단 한 프레임도 안 움직였다 — front 는 `fatal_max_run` 도 16 으로
  같아, 라벨 붙은 누움/측면 프레임 전부가 이미 하나의 연속 구간으로 잡혀 있다는
  뜻이다. 원인은 conf_min 이 아니라 이 데이터셋의 라벨 구간 길이다: `segments_draft.md`
  의 `--lying` 구간은 전부 약 1초(15~16프레임)인데, `PostureGate` 의 미결정 유예는
  `UNKNOWN_STOP_FRAMES=25`프레임(≈1.7초, `constants.py:42`)이다. 즉 라벨 구간이 유예
  창보다 짧아서, 그 구간 안에서 분류가 `Standing`/`Unknown`/`Lying` 중 무엇이 나오든
  게이트가 정지로 넘어갈 시간 자체가 없다 — 이 스윕이 `fatal_frames` 를 못 움직인 건
  conf_min 이 무해해서가 아니라 이 표본의 누움 구간이 전부 유예창보다 짧기 때문이다.
- 이 데이터만으로 "즉시 정지"를 켜도 된다고 결론 내리지 않는다. 그러려면 위에 적은
  운영 로깅이 먼저 필요하다는 것을 결과에 명시한다: `fatal_frames` 가 이 스윕 내내
  고정값(16/16)으로 나온 것 자체가 그 근거다 — 이 표는 "1초 안팎의 누움"에서 게이트가
  뚫리는지만 보여줄 수 있을 뿐, 실제 운영에서 문제가 되는 "유예창(1.7초)보다 긴 누움을
  얼마나 늦게 잡는가"·"정상 추종 중 오탐 정지가 분당 몇 번인가"는 이 저장소에 그런
  로깅이 없어 이 Task 범위에서 잴 수 없다. `conf_min` 을 낮추는 결정은 이 트레이드오프
  표(오름세인 torso4_pass·jitter, 계단형인 accuracy) 만으로는 못 내리고, 그 운영
  로깅이 먼저 있어야 한다.

## 모델 비교 (conf_min = 0.50 고정)

⚠️ **conf_min = 0.50 을 쓰는 이유**: 위 판정 절이 "즉시 정지"뿐 아니라 `conf_min` 자체를
낮추자는 결정도 이 트레이드오프 표만으로는 못 내린다고 명시한다(운영 로깅 인프라가
먼저 필요). 즉 Task 6 은 새 값을 "고르지" 않았다 — 현재 배포값(0.50)에서 벗어날 근거가
없다는 것이 결론이다. 그래서 이 모델 비교도 현재값 0.50 을 고정해서 잰다.

| model | torso4_pass front/back | accuracy front/back | jitter front/back | ms/frame front/back |
|---|---|---|---|---|
| yolo11n-pose (현재) | 0.687 / 0.422 | 0.197 / 0.263 | 0.185 / 0.143 | 12.3 / 8.8 |
| rtmpose-m-body7 | 0.841 / 0.700 | 0.372 / 0.421 | 0.141 / 0.083 | 24.0 / 23.2 |

⚠️ 2026-07-31 보고서가 경고한 대로 대조군이 비대칭이고, 피사체가 사람이 아니라
피규어다. 이 표는 "같은 데이터에서 어느 쪽이 나은가"만 말하며 현장 전이를 보장하지 않는다.

### 이 표로 결정할 수 있는 것

⚠️ Task 6과 같은 한계다 — `torso4_pass` 개선폭은 "즉시 정지"의 근거가 아니다.
연속 `Unknown` 길이 분포·분당 오탐정지 같은 운영 지표가 없으면 배포 결정을 못
내린다(Task 6 "판정" 절 참고). 이 표는 **모델 선택**(더 나은 후보가 있는가)에만 쓴다.

- 두 모델 중 이 클립·이 conf_min 조건에서 어느 쪽이 나은가: `rtmpose-m-body7`이 이 표의
  네 지표 전부에서 두 캠 모두 이긴다. `torso4_pass` front 0.687→0.841(+0.154p, 상대
  +22%) / back 0.422→0.700(+0.278p, 상대 +66% — 뒷캠이 절대·상대 모두 더 이득이고,
  Task 6의 conf_min 스윕에서 뒷캠이 더 크게 반응한 것과 같은 방향). `accuracy` front
  0.197→0.372(+0.175p) / back 0.263→0.421(+0.158p). `jitter_torso4`(낮을수록 좋음)
  front 0.185→0.141(상대 -24%) / back 0.143→0.083(상대 -42%).
- 그 개선폭이 ms/frame 예산(66ms @15fps) 안에서 나오는가: 그렇다. `rtmpose-m-body7`은
  front 24.0ms / back 23.2ms로 `yolo11n-pose`(12.3ms / 8.8ms) 대비 약 2.0~2.6배
  느리지만, 66ms 예산 대비 2.75~2.84배 여유가 남는다. 단 이 ms/frame은 이 벤치가 재는
  "crop → keypoint" pose 어댑터 구간만이고(`pose_bench.py`의 `_run_model`), crop을
  만드는 detector(`best.pt`) 비용은 포함하지 않는다 — 실제 온보드 파이프라인 전체
  지연은 이 값보다 크다.
- (표 밖·같은 실행에서 나온 값, 참고용) `fatal_frames`는 반대 방향이다 —
  `rtmpose-m-body7`이 오히려 더 나쁘다: front 16→53, back 16→33(같은 `PostureGate`,
  같은 라벨 구간, 모델만 다름). 이 표의 네 지표가 전부 이겨도 "누움 구간에서 게이트가
  주행을 허용한 프레임 수"는 줄지 않고 늘었다 — 모델 교체 근거를 약화시키는 신호이며,
  왜 그런지는 이 Task 범위에서 조사하지 않았다.
- 결론(현장 재촬영·실측 없이 모델을 바꿀 근거가 되는가, 아니면 추가 실측이 먼저인가):
  추가 실측이 먼저다. `rtmpose-m-body7`이 이 클립·이 conf_min에서 `torso4_pass`·
  `accuracy`·`jitter` 세 지표 모두를 이기고 ms/frame 예산도 여유 있다는 것은 재촬영·
  실측에 투자할 값어치가 있는 후보라는 근거는 되지만, ① 대조군 비대칭(모듈 머리말 —
  body7 승은 도메인 효과와 데이터량 효과를 구분 못 함), ② 피사체가 사람이 아니라
  피규어, ③ 방금 위에서 본 `fatal_frames` 역전까지 겹쳐 있어 이 숫자만으로 배포
  모델을 바꾸는 근거로는 부족하다.
