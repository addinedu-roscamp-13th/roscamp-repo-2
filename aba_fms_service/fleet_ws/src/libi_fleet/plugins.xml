<library path="libi_fleet_plugins">
  <class type="libi_fleet::Auction" base_class_type="libi_fleet::DispatcherBase">
    <description>Auction / SSI dispatch — lowest Dijkstra path-cost bid wins (single-task now; batch queue extends to true SSI).</description>
  </class>
  <class type="libi_fleet::ReservationDeadlock" base_class_type="libi_fleet::TrafficBase">
    <description>교통 — 노드 예약 + wait-for 그래프 DFS 교착감지. 목표 노드를 먼저 확보한 뒤 출발 노드를 놓으므로 노드예약만으로 정면충돌·후미추돌이 막힌다. 실사용 기본값.</description>
  </class>
  <class type="libi_fleet::GrantAllTraffic" base_class_type="libi_fleet::TrafficBase">
    <description>[experimental] Traffic OFF — always GRANT, no reservation/deadlock. Diagnostic only, unsafe for real robots.</description>
  </class>
</library>
